{
  "name": "airline-flightstatus-agent-001",
  "displayName": "FlightStatus",
  "description": "Sub-agent that handles flight status inquiries.",
  "instruction": "agents/FlightStatus/instruction.txt",
  "tools": ["end_session"],
  "toolsets": [{
    "toolset": "big_query",
    "toolIds": ["execute_sql", "execute_sql_readonly"]
  }]
}