{
  "name": "airline-booking-agent-001",
  "displayName": "Booking",
  "description": "Sub-agent that handles flight bookings and reservations.",
  "instruction": "agents/Booking/instruction.txt",
  "tools": ["end_session"],
  "toolsets": [{
    "toolset": "big_query",
    "toolIds": ["execute_sql"]
  }]
}